<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OrderMapper">

    <select id="innerJoinOrders" resultMap="Order">
        select "ORDER".ID, user.NAME, goods.NAME as goods_name, "ORDER".GOODS_NUM*"ORDER".GOODS_PRICE as total_price from "ORDER"
        inner join user on "ORDER".USER_ID = USER.ID
        inner join GOODS on "ORDER".GOODS_ID = GOODS.ID
    </select>

    <resultMap id="Order" type="Order">
        <result property="id" column="id"/>
        <result property="totalPrice" column="total_price"/>
        <association property="user" javaType="User">
            <result property="name" column="name"/>
        </association>
        <association property="goods" javaType="com.github.hcsp.mybatis.entity.Goods">
            <result property="name" column="goods_name" />
            <result property="price" column="goods_price" />
        </association>
    </resultMap>
</mapper>