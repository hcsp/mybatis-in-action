<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OrderMapper">
    <select id="getInnerJoinOrder" resultMap="Order">
        select `ORDER`.ID as order_id, user.id as user_id, user.name as user_name, user.tel as user_tel,
        user.address as user_address, goods.id as goods_id, goods.name as goods_name,
        `ORDER`.GOODS_PRICE as goods_price, GOODS_NUM * `ORDER`.GOODS_PRICE AS total_price from `ORDER`
        JOIN USER ON `ORDER`.USER_ID=USER.ID
        JOIN GOODS ON `ORDER`.GOODS_ID=GOODS.ID
    </select>
    <resultMap id="Order" type="com.github.hcsp.mybatis.entity.Order">
        <result property="id" column="order_id"/>
        <result property="totalPrice" column="total_price"/>
        <association property="user" javaType="User">
            <result property="id" column="user_id"/>
            <result property="name" column="user_name"/>
            <result property="tel" column="user_tel"/>
            <result property="address" column="user_address"/>
        </association>
        <association property="goods" javaType="com.github.hcsp.mybatis.entity.Goods">
            <result property="id" column="goods_id"/>
            <result property="name" column="goods_name"/>
            <result property="price" column="goods_price"/>
        </association>
    </resultMap>
</mapper>