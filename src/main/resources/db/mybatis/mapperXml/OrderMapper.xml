<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.hcsp.mybatis.mapper.OrderMapper">
    <resultMap id="orderMap" type="order">
        <id property="id" column="ID" />
        <result property="totalPrice" column="TOTAL_PRICE"/>
        <association property="user" javaType="User">
            <id property="id" column="USER_ID"/>
            <result property="name" column="USER_NAME"/>
            <result property="tel" column="TEL"/>
            <result property="address" column="ADDRESS"/>
        </association>
        <association property="goods" javaType="Goods">
            <id property="id" column="GOODS_ID"/>
            <result property="name" column="GOODS_NAME"/>
            <result property="price" column="PRICE"/>
        </association>
    </resultMap>
    <select id="getInnerJoinOrders" resultMap="orderMap">
        SELECT "ORDER".ID,USER.NAME AS USER_NAME,GOODS.NAME AS GOODS_NAME,
                GOODS_NUM * GOODS_PRICE AS TOTAL_PRICE
                FROM "ORDER"
                INNER JOIN USER ON USER.ID = USER_ID
                INNER JOIN GOODS ON GOODS.ID = GOODS_ID;
    </select>
</mapper>