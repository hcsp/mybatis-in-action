<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="getUserByPage" resultType="User">
        select id, name, tel, address from user
        <if test="username != null">
            where name = #{username}
        </if>
        limit #{offset}, #{pageSize}
    </select>
    <select id="getTotalPage" parameterType="String" resultType="int">
        select count(*) from user
        <if test="username != null">
            where name = #{username}
        </if>
    </select>
    <insert id="batchInsertUsers">
        insert into user (name, tel, address) values
        <foreach collection="users" item="user" separator=",">
            (#{user.name}, #{user.tel}, #{user.address})
        </foreach>
    </insert>
    <update id="updateUser">
        update user
        <set>
            <if test="name != null">name=#{name},</if>
            <if test="tel != null">tel=#{tel},</if>
            <if test="address != null">address=#{address},</if>
        </set>
        where id=#{id}
    </update>
    <select id="selectUserById" parameterType="int" resultType="User">
        select id, name, tel, address from user where id = #{id}
    </select>
    <delete id="deleteUserById" parameterType="int">
        delete from user where id = #{id}
    </delete>
    <select id="getInnerJoinOrders" resultMap="getInnerJoinOrdersResultMap">
        select o.id id, (o.goods_num * o.goods_price) as totalPrice,
        u.name as user_name, u.id as user_id, u.tel as user_tel, u.address as user_address,
        g.name as goods_name, g.price as goods_price, g.id as goods_id
        from `order` o
        join goods g on o.goods_id = g.id
        join user u on o.user_id = u.id
        where g.name is not null and u.name is not null
    </select>
    <resultMap id="getInnerJoinOrdersResultMap" type="Order">
        <id property="id" column="id"></id>
        <result property="totalPrice" column="totalPrice"/>
        <association property="user" javaType="User">
            <id property="id" column="user_id"/>
            <result property="name" column="user_name"/>
            <result property="tel" column="user_tel"/>
            <result property="address" column="user_address"/>
        </association>
        <association property="goods" javaType="Goods">
            <id property="id" column="goods_id"/>
            <result property="name" column="goods_name"/>
            <result property="price" column="goods_price"/>
        </association>
    </resultMap>
</mapper>