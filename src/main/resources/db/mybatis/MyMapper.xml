<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MyMapper">
    <select id="getUserByPage" parameterType="map" resultType="User">
        select ID,NAME ,TEL,ADDRESS from USER
        <if test="username != null">
            where NAME = #{username}
        </if>
        limit #{offset},#{limit}
    </select>
    <select id="countUser" parameterType="String" resultType="int">
        select count(*)
        from user
        <if test="username != null">
            where NAME = #{username}
        </if>
    </select>
    <insert id="batchInsertUsers" parameterType="map">
        insert into user(NAME ,TEL,ADDRESS)
        values
        <foreach item="user" index="index" collection="users"
                 separator=",">
            (#{user.name},#{user.tel},#{user.address})
        </foreach>
    </insert>
    <update id="updateUser" parameterType="User">
        update USER
        <set>
            <if test="name != null">name=#{name},</if>
            <if test="tel != null">tel=#{tel},</if>
            <if test="address != null">address=#{address},</if>
        </set>
        where id=#{id}
    </update>
    <delete id="deleteUserById" parameterType="int">
        delete
        from user
        where id = #{id}
    </delete>
    <select id="selectUserById" parameterType="int" resultType="user">
        select *
        from USER
        where ID = #{id}
    </select>
    <select id="getInnerJoinOrders" resultMap="Orders">
select "ORDER".ID as ORDER_ID,
        USER.NAME as USER_NAME, GOODS.NAME as GOODS_NAME,
        GOODS.PRICE as GOODS_PRICE, "ORDER".GOODS_NUM as GOODS_NUM,
        ("ORDER".GOODS_PRICE * "ORDER".GOODS_NUM) AS TOTAL_PRICE
from "ORDER"
         inner join GOODS on "ORDER".GOODS_ID = GOODS.ID
         inner join USER on "ORDER".USER_ID = USER.ID
WHERE GOODS.NAME is not null and USER.NAME is not null
    </select>

    <resultMap id="Orders" type="Order">
        <result property="totalPrice" column="total_price"/>
        <result property="id" column="order_id"/>
        <association property="user" javaType="User">
            <id property="id" column="user_id"/>
            <result property="name" column="user_name"/>
        </association>
        <association property="goods" javaType="Goods">
            <id property="id" column="goods_id"/>
            <result property="name" column="goods_name"/>
            <result property="price" column="price"/>
        </association>
    </resultMap>
</mapper>