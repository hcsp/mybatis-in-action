<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="db.mybatis.daoMapperWithXML">
    <select id="getTotalOfUser" resultType="int">
        select count(*) from USER
        <if test="name != null">
            where name=#{name}
        </if>
    </select>
    <update id="updateUser">
        update USER
        <set>
            <if test="name != null">name=#{name},</if>
            <if test="address != null">address=#{address},</if>
            <if test="tel != null">tel=#{tel},</if>
        </set>
        where id = #{id}
    </update>
    <select id="getUserByPages" resultType="com.github.hcsp.mybatis.entity.User">
        select * from USER
        <if test="name != null">
            where name=#{name}
        </if>
    </select>
    <insert id="batchInsertUsers">
        insert into USER (name, tel, address)
        values (#{name}, #{tel}, #{address})
    </insert>


    <resultMap id="goodsMap" type="Goods">
        <result column="goodsId" property="id" />
        <result column="goodsName" property="name" />
        <result column="goodsPrice" property="price" />
    </resultMap>

    <resultMap id="orderMap" type="Order">
        <result column="id" property="id" />
        <result column="totalPrice" property="totalPrice" />
        <association property="user" javaType="User">
            <result property="id" column="userId"/>
            <result property="name" column="username"/>
            <result property="tel" column="userTel"/>
            <result property="address" column="userAddress"/>
        </association>
        <collection property="goods" resultMap="goodsMap"/>
    </resultMap>


    <select id="getInnerJoinOrders" resultMap="orderMap">
        select
               "ORDER".ID as id,
               USER.ID as userId,
               USER.NAME as userName,
               USER.TEL as userTel,
               USER.ADDRESS as userAddress,
               GOODS.ID as goodsId,
               GOODS.NAME as goodsName,
               GOODS.PRICE as goodsPrice,
               CAST("ORDER".GOODS_PRICE * "ORDER".GOODS_NUM as decimal) as totalPrice
        from "ORDER"
                 join GOODS on GOODS.ID = "ORDER".GOODS_ID
                 join USER on USER.ID = "ORDER".USER_ID;
    </select>
</mapper>


